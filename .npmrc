# Pin exactly on `pnpm add`, so a version is only ever raised deliberately —
# by Dependabot's monthly PR or by hand — never by a range resolving to
# something newer. Without this, pnpm writes ^ by default.
save-exact=true
